/**
 * This is a dummy file to force the IDE to refresh its cached state.
 * The presence of this file should help trigger a cache refresh.
 * 
 * Several phantom references to non-existent files like "SupplierForm-updated.svelte"
 * are causing TypeScript errors even though the actual files are correct.
 */

// Force TypeScript to treat this as a module
export const resetCacheTimestamp = Date.now();
